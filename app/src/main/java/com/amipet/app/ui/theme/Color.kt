package com.amipet.app.ui.theme

import androidx.compose.ui.graphics.Color

// Cores primárias
val Primary = Color(0xFFE2F986)
val PrimaryVariant = Color(0xFF388E3C)
val OnPrimary = Color(0xFFFFFFFF)

// Cores secundárias
val Secondary = Color(0xFF8BC34A)
val SecondaryVariant = Color(0xFF689F38)
val OnSecondary = Color(0xFF000000)

// Cores de fundo
val BackgroundLight = Color(0xFFFFFFE6)
val BackgroundDark = Color(0xFF121212)
val OnBackgroundLight = Color(0xFF000000)
val OnBackgroundDark = Color(0xFFFFFFFF)

// Cores de superfície
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF1E1E1E)
val OnSurfaceLight = Color(0xFF000000)
val OnSurfaceDark = Color(0xFFFFFFFF)

// Cores de erro
val Error = Color(0xFFF44336)
val OnError = Color(0xFFFFFFFF)

// Cores de ação
val LikeAction = Color(0xFF7FC8F8)
val DislikeAction = Color(0xFFF4976C)

// Cores de borda
val CardBorderLight = Color(0xFFE0E0E0)
val CardBorderDark = Color(0xFF3A3A3A)
